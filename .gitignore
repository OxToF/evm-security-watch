# Environment / secrets — NEVER commit
.env
.env.*
!.env.example
*.env
.envrc

# API keys, RPC endpoints, private keys
*secret*
*credentials*
*.pem
*.key
*-keypair.json
keystore/
wallet*.json

# Foundry / Hardhat build artifacts
out/
cache/
artifacts/
broadcast/
typechain-types/

# Node / tooling
node_modules/
dist/
.cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Scanner / watch runtime output
scan-out/
watch-reports/
